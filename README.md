# Aleksandr B. Ivanov's dot files

These are config files to set up a system the way I like it.

### Setup

```sh
$ git clone git@github.com:radanisk/dotfiles.git
$ cd dotfiles
$ make nvim-install
$ make dotfiles-install
```
